#!/bin/bash
git commit -m "chore(version): Tick dashboard version to 2.1.3 for testing

Incremented the dashboard version to ensure the state persistence fix is testable and not affected by browser caching."
#!/bin/bash
git commit -m "chore(version): Tick dashboard version to 2.1.2 for testing

Incremented the dashboard version to ensure the state management fixes are testable and not affected by browser caching."
